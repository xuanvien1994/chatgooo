<!-- <p id="notice"><%= notice %></p>

<h1 >Friendships</h1>

<table class="form-box">
  <thead>
      <th>Friend Name</th>
      <th colspan="3"></th>
  </thead>

  <tbody>
    <% @friendships.each do |friendship| %>
      <tr>
        <td><%= friendship.friend_id %></td>
      </tr>
    <% end %>
    <% @friendname.each do |friendname| %>
          <tr>
            <td><%= friendname.username %></td>
          </tr>
        <% end %>
  </tbody>
</table>

<br> -->

<div class="form-bigbox">
  <div class="form-box">
<div id="stream">

    <div id="stream-list" class="follow-list">
      <ul>   
        <% @friendname.each do |friendname| %>
          <label for="friendname<%= friendname.id %>" >
            <li id="stream_item">
              <span class="list-group-item"><%= friendname.username %></span>
            </li>
          </label>
          <br>
        <% end %>
      </ul>
    </div>

    <div id ="stream-footer">
    </div>
    </div>
    </div>
</div>

